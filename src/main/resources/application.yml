# ?? ?? ?? (???? 8080)
server:
  port: 8080

# --- ?????? ? JPA ?? ---
spring:
  # ??? ?? ?? (AWS RDS MySQL ?? ??)
  datasource:
    driver-class-name: com.mysql.cj.jdbc.Driver
    # RDS ????? ??? ??
    url: jdbc:mysql://my-board-db.cd8u22a00667.ap-northeast-2.rds.amazonaws.com:3306/board?useSSL=false&serverTimezone=UTC&characterEncoding=UTF-8
    # RDS ?? ? ??? ??? ??? ???? ??
    username: admin
    # RDS ?? ? ??? ??? ????? ??
    password: kimsungmin1!

  # JPA (Java Persistence API) ??
  jpa:
    hibernate:
      ddl-auto: update

    properties:
      hibernate:
        # ???? SQL ??? ?? ?? ???
        format_sql: true
        # SQL ??? ???? ???? ?? ??? ??
        show_sql_parameters: true

    # ??? ???? SQL ??? ??
    show-sql: true

# ?? ?? ?? (JPA? ???? SQL? ? ??? ?? ??)
logging:
  level:
    # org.hibernate.SQL: debug
    org.hibernate.type.descriptor.sql: trace